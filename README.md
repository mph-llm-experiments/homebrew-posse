# homebrew-posse

Homebrew tap for [POSSE](https://github.com/mph-llm-experiments/posse).

```sh
brew tap mph-llm-experiments/posse
brew install --cask posse
```

The app is unsigned. The cask strips the macOS quarantine attribute on
install so the app launches without right-click → Open.
